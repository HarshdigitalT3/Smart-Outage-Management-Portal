# Frontend environment configuration (Vite)
# Copy to .env and fill in values.

# Base URL of the backend API, e.g. http://localhost:4000
VITE_API_BASE_URL=

# Google Maps API key used by the operator Live Map view.
# Create in Google Cloud Console and enable "Maps JavaScript API".
VITE_GOOGLE_MAPS_API_KEY=
