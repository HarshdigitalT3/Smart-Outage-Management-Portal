declare module "nodemailer" {
  // Minimal typing to satisfy TypeScript in this scaffolded repo.
  // If full typings are desired, install @types/nodemailer and remove this file.
  const nodemailer: any;
  export default nodemailer;
}
